{
  "name": "lin-dong-academic-website",
  "private": true,
  "version": "1.0.0",
  "description": "Personal academic website — static HTML with a compiled Tailwind CSS build.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./assets/css/site.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./assets/css/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
