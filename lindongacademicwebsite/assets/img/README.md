Add your images here, for example:

- `profile.jpg` — headshot used on the Home page hero (replace the "LD" placeholder block in `index.html`)
- `research.jpg` — optional image for the Research Impact section in `publications.html`

After adding a file, update the corresponding `<img>`/placeholder markup in the HTML to point to it.
